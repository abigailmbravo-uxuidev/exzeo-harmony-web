version: "2"
services:
  harmony-csr:
    build:
      context: .
      dockerfile: Dockerfile
      args:
      - NPM_TOKEN=ENTER_NPM_TOKEN_HERE
    command: npm run server
    container_name: harmony-web
    environment:
      - PORT=80
      - API_URL=https://api.harmony-ins.com
      - AUTH0_PRIMARY_URL=http://devcsr.harmony-ins.com:8383
      - AQA_SSO_URL=https://www-qa.typtap.com/agency/agents/sso
      - AUTH0_DOMAIN=ashton-sandbox.auth0.com
      - AUTH0_CLIENT_ID=_aVZ55R37kcyZ3NtQcps0bwZ09YYdPLd
      - AUTH0_CONNECTION=ashton-sandbox
      - AUTH0_AUDIENCE=http://harmony-sandbox
      - REACT_APP_VERSION="1.0"
    ports:
      - "8383:80"
